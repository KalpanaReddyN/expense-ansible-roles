- name: configuring mysql server
  hosts: mysql
  become: yes  #remote server
  vars_files:
  - /var/expense/credentials_mysql.yaml # this is present in ansible server contains passwords.
  roles:
  - mysql